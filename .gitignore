# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Yarn (node-modules linker: only its local install cache lands here)
.yarn/
.pnp.*

# Generated by install-shortcut.sh from the .template — contains this machine's absolute path
claude-session-manager.desktop

# Per-machine preferences for the "New task" modal (default prompt, branch types, worktree-by-default
# checkbox) — local preference, not shared project content.
/userPreferences.json

# Local sidecar data — session nicknames, "Compact & continue" links, and "New task" worktrees'
# recorded base branches. Per-machine, not shared project content, same reasoning as
# userPreferences.json above (see nicknames.ts/sessionContinuations.ts/taskBaseBranches.ts).
/custom-titles.json
/session-continuations.json
/task-base-branches.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.claude/*
