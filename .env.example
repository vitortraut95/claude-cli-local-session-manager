# Copy to .env and fill in values relevant to your own setup. .env is gitignored — per-machine
# config, not shared project content.

# Base URL of the Jenkins instance used by the "Open Jenkins" button on session cards (only
# rendered for env/*-prefixed branches). Leave unset to hide that button entirely.
VITE_JENKINS_BASE_URL=
